import { RingBuffer } from './ring-buffer.js';
import type { HermesTarget, ConsoleEntry, NetworkEntry, CDPClientState, EvaluateResult } from './types.js';
export declare class CDPClient {
    private ws;
    private msgId;
    private pending;
    private eventHandlers;
    private _consoleBuffer;
    private _networkBuffer;
    private _port;
    private reconnecting;
    private disposed;
    private _helpersInjected;
    private _networkMode;
    private _isPaused;
    private _connectedTarget;
    private _state;
    private _connectionGeneration;
    constructor(port?: number);
    get state(): CDPClientState;
    get isConnected(): boolean;
    get isPaused(): boolean;
    get helpersInjected(): boolean;
    get metroPort(): number;
    get connectedTarget(): HermesTarget | null;
    get networkMode(): 'cdp' | 'hook' | 'none';
    get consoleBuffer(): RingBuffer<ConsoleEntry>;
    get networkBuffer(): RingBuffer<NetworkEntry>;
    get connectionGeneration(): number;
    autoConnect(portHint?: number): Promise<string>;
    disconnect(): Promise<void>;
    evaluate(expression: string, awaitPromise?: boolean): Promise<EvaluateResult>;
    send(method: string, params?: unknown): Promise<unknown>;
    private connectToTarget;
    private connectWs;
    private handleMessage;
    private parseNetworkHookMessage;
    private setup;
    private setupEventHandlers;
    private waitForReact;
    private handleClose;
    private reconnect;
    private rejectAllPending;
    private sendWithTimeout;
    private sleep;
}
