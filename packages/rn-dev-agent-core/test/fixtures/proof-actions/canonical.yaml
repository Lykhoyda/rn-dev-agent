appId: com.example.proof
---
# id: canonical-proof
# intent: Exercise strict-proof action identity without an application checkout.
# tags: [proof, synthetic]
# mutates: false
# status: active
# expectedRouteSequence: [ProofStart, ProofResult]
- assertVisible:
    id: proof-start
- tapOn:
    id: proof-continue
- assertVisible:
    id: proof-result
