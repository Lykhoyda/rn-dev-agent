{
  "name": "qaren-android-runner",
  "version": "0.0.0",
  "private": true,
  "description": "Android UiAutomator runner source project for QaReN device control.",
  "license": "MIT",
  "scripts": {
    "build": "./gradlew assembleDebug assembleDebugAndroidTest --no-daemon",
    "test": "./gradlew testDebugUnitTest --no-daemon"
  }
}
