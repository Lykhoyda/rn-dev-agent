{
  "name": "rn-dev-agent-android-runner",
  "version": "0.0.0",
  "private": true,
  "description": "Android UiAutomator runner source project for rn-dev-agent device control.",
  "license": "MIT",
  "scripts": {
    "build": "./gradlew assembleDebug assembleDebugAndroidTest --no-daemon",
    "test": "./gradlew testDebugUnitTest --no-daemon"
  }
}
