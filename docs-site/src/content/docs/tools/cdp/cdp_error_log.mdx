---
title: "cdp_error_log"
description: "Get unhandled JS errors and promise rejections"
sidebar:
  order: 17
---

Get unhandled JS errors and promise rejections. Hooked into ErrorUtils and Hermes rejection tracker. If empty but app crashed, the error is NATIVE — call cdp_native_errors to check native logs.

## Parameters

| Name | Type | Required | Default | Constraints | Description |
|------|------|----------|---------|-------------|-------------|
| `clear` | `boolean` | No | `false` |  | Clear all captured errors instead of reading them |

## Usage

```
cdp_error_log()
```
