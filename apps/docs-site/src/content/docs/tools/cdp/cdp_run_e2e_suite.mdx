---
title: "cdp_run_e2e_suite"
description: "Run locked e2e tests strictly on the authority-bound session device and persist a session-scoped report"
---

Run locked e2e tests strictly on the authority-bound session device and persist a session-scoped report.

## Parameters

| Name | Type | Required | Default | Constraints | Description |
|------|------|----------|---------|-------------|-------------|
| `pattern` | `string` | No |  |  | Regex filter over locked-test ids |
| `projectRoot` | `string` | No |  |  |  |
| `deviceId` | `string` | No |  |  |  |

## Usage

```
cdp_run_e2e_suite()
```
