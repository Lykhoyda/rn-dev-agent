---
'rn-dev-agent-core': patch
'rn-dev-agent-plugin': patch
---

Allow the managed-Metro Darwin sandbox to write the resolved, run-owned `react-native-css-interop/.cache` directory while preserving all other sandbox restrictions and enforcement checks.
