---
'qaren': major
---

Rename the plugin to QaReN and cut it to three packages: the `qaren` CLI, a `qaren-core` screen layer that builds and tests without the MCP server or the session stack, and a `qaren-plugin` host package with five skills; the shared-knowledge mirror, the Codex package, the committed host bundles and their sync gates are removed.
