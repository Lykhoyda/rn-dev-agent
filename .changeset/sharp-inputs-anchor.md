---
'rn-dev-agent-core': patch
'rn-dev-agent-plugin': patch
---

Anchor `cdp_interact typeText` resolution at the matched selector fiber so real app screens resolve within the unchanged work budget, and tell `device_fill` callers when an accessible iOS wrapper has merged the inner input away.
