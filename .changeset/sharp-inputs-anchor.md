---
'rn-dev-agent-core': patch
'rn-dev-agent-plugin': patch
---

Anchor `cdp_interact typeText` resolution at the matched selector fiber and treat the composite fibers that own a single host text input as one semantic input, so real app screens resolve and dispatch once within the unchanged work budget, while genuinely distinct inputs still refuse as ambiguous; `device_fill` now names the iOS accessible-wrapper merge instead of implying a snapshot rebind.
