---
"rn-dev-agent-core": patch
"rn-dev-agent-plugin": patch
---

Launch Expo session builds with a command shape the installed Expo CLI accepts and release pending build authority through an authenticated abort when the native command fails before completion.
