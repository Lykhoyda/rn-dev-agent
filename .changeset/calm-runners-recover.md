---
"rn-dev-agent-core": patch
"rn-dev-agent-plugin": patch
---

Allow a failed deterministic login replay to rebind its exact already-owned runner through a non-destructive attach-only open before retrying the guarded login sequence.
