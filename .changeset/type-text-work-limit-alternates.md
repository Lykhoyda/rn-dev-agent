---
'rn-dev-agent-plugin': patch
'rn-dev-agent-core': patch
---

Raise React-tree `typeText` and input read-back to a shared 500,000-work-unit limit (a measured diagnostic, not a performance recommendation) and make TextInput designation recognize React alternates.
