---
'rn-dev-agent-plugin': patch
---

`/qa-pr` posts a human-reviewer comment (the feature, the verdict, the commit and platform tested, the video, what was tested, repro steps) and then removes the `needs-qa` label when it is present.
