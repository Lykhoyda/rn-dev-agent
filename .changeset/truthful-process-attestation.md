---
'rn-dev-agent-core': patch
'rn-dev-agent-plugin': patch
---

Preserve process-attestation failure causes so unavailable, mismatched, and absent identities remain distinguishable under existing fail-closed refusal codes.
