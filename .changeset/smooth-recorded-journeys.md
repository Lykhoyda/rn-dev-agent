---
'rn-dev-agent-plugin': patch
'rn-dev-agent-core': patch
---

Normalize native recordings to 30 fps H.264 while preserving captured timing and cap them at a 720 px short edge, so idle stretches play as smooth video instead of a slideshow.
