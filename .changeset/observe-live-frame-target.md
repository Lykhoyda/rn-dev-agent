---
'rn-dev-agent-core': patch
'rn-dev-agent-plugin': patch
---

Resolve the Observe Device pane's live frame through the same authority-fenced device target as the mirror, so a parked runner or stale CDP no longer leaves the pane blank after `cdp_run_action`.
