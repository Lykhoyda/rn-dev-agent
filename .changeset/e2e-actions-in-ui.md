---
"rn-dev-agent-cdp": minor
---

feat(e2e): Actions panel in the observe page — list the project's actions and run any one (repairable `cdp_run_action`) with params resolved from `.rn-agent/e2e.config.json`, via `GET /api/e2e/actions` + `POST /api/e2e/actions/run`.
