---
"rn-dev-agent-core": patch
"rn-dev-agent-plugin": patch
---

Scope Android element matching to the owned app by default, actuate exact accessibility targets without trusting snapshot coordinates, and fail taps whose effect cannot be proven.
