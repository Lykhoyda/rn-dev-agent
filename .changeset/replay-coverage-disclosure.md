---
'rn-dev-agent-plugin': patch
'rn-dev-agent-core': patch
---

The React-tree replay refusal `frontmost proof cannot cover every mounted renderer` now carries `meta.coverage` with its reasons and renderer ids.
