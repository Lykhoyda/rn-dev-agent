---
'qaren': minor
---

Add `qaren check`, which leases the booted simulator, builds and launches the working tree, spawns the core walker over a one-line-per-JSON wire, walks a literal plan with exact targets and quoted ✓ lines, and writes a ledger and report under `~/.qaren/runs/<run-id>/`.
