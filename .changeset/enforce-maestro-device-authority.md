---
"rn-dev-agent-core": patch
"rn-dev-agent-plugin": patch
---

Bind Maestro replays to the exact active device, reject mismatched direct runner or WDA provenance, and persist RunRecord device identity from execution evidence instead of requested metadata.
