---
"rn-dev-agent-core": patch
---

Give authenticated managed-Metro descendants an open stdin so Tailwind-backed transforms cannot stall, and fail typed instead of hanging when a child's first authenticated exchange never completes.
