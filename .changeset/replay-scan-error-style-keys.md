---
'rn-dev-agent-plugin': patch
'rn-dev-agent-core': patch
---

The React-tree replay refusal's `meta.coverage` now names the phase and message of each scan error, and the frontmost prover reads style keys directly so styles it cannot enumerate no longer abort the scan.
